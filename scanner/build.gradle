plugins {
    // Apply core plugins.
    id 'java-library'
    id 'application'
}

applicationName = 'cache-cleanup'
mainClassName = 'com.here.ort.scanner.CacheCleanupKt'

dependencies {
    api project(':model')

    implementation project(':downloader')
    implementation project(':utils')
}
